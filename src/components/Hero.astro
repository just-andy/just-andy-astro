---
import { Image } from "astro:assets";

const { url, title, summary } = Astro.props;
---

<div class="hero">
  <Image src={url} alt="" />
  <div class="hero__content">
    <h1>{title}</h1>
    <div>{summary}</div>
  </div>
</div>
